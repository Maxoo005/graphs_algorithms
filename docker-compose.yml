version: "3.9"

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aizo_p2_dev
    volumes:
      - .:/project
    working_dir: /project
    stdin_open: true
    tty: true
